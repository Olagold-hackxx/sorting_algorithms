#include "sort.h"

/**
* quick_sort - sort with Selection sort algorithm
* @array: array to sort
* @size: size of array
*/
void quick_sort(int *array, size_t size)
{
	
}

int partition(int lowIndex, int highIndex)